"""Unhealthy module - high complexity, deep nesting, no docs, large size."""


def complex_function(x, y, z, w, a, b, c, d):
    if x > 0:
        if y > 0:
            if z > 0:
                if w > 0:
                    if a > 0:
                        if b > 0:
                            if c > 0:
                                if d > 0:
                                    return x + y + z + w + a + b + c + d
    return 0


def another():
    pass


def yet_another():
    pass


def one_more():
    pass


def and_another():
    pass
