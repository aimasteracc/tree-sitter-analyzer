[pytest]
markers =
    unit: mark test as unit test
    requires_ripgrep: mark test as requiring ripgrep (rg) command
    requires_fd: mark test as requiring fd command
    integration: mark test as integration test
    performance: mark test as performance test
    slow: mark test as slow running
    network: mark test as requiring network access
    asyncio: mark test as asyncio test
    mcp: mark test as MCP-related test
    requires_python: mark test as requiring tree-sitter-python
    requires_java: mark test as requiring tree-sitter-java

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts = 
    --strict-markers
    --strict-config
    --verbose
    --tb=short
    --maxfail=10
    --durations=10

# Minimum version
minversion = 6.0

# Filter warnings
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::UserWarning:tree_sitter.*
