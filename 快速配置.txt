================================================================================
Tree-Sitter Analyzer V2 - Cursor 快速配置
================================================================================

项目路径: C:/git-private/tree-sitter-analyzer-workspace/tree-sitter-analyzer-v2

================================================================================
步骤 1: 安装依赖
================================================================================

cd C:/git-private/tree-sitter-analyzer-workspace/tree-sitter-analyzer-v2
uv pip install -e ".[mcp]"

================================================================================
步骤 2: 验证安装
================================================================================

cd C:/git-private/tree-sitter-analyzer-workspace
uv run --directory tree-sitter-analyzer-v2 python test_mcp_server.py

应该看到: [SUCCESS] All tests passed!

================================================================================
步骤 3: 复制以下配置到 Cursor
================================================================================

{
  "mcpServers": {
    "tree-sitter-analyzer-v2": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "C:/git-private/tree-sitter-analyzer-workspace/tree-sitter-analyzer-v2",
        "python",
        "-m",
        "tree_sitter_analyzer_v2.mcp"
      ],
      "env": {
        "TREE_SITTER_PROJECT_ROOT": "C:/git-private/tree-sitter-analyzer-workspace/tree-sitter-analyzer-v2"
      },
      "timeout": 3600,
      "disabled": false
    }
  }
}

================================================================================
步骤 4: 重启 Cursor
================================================================================

配置完成后，重启 Cursor 使配置生效。

================================================================================
使用示例
================================================================================

在 Cursor AI 对话中:

1. "分析 tree_sitter_analyzer_v2/mcp/server.py 文件"
2. "找出所有 Python 文件"
3. "搜索代码中的 TODO"
4. "查找调用 handle_request 的地方"
5. "可视化代码结构"

================================================================================
可用工具 (11 个)
================================================================================

✓ analyze_code_structure  - 分析代码结构
✓ query_code             - 查询代码元素
✓ check_code_scale       - 检查代码规模
✓ extract_code_section   - 提取代码片段
✓ find_files            - 查找文件 (超快)
✓ search_content        - 搜索内容 (超快)
✓ find_and_grep         - 组合搜索
✓ analyze_code_graph    - 分析代码图谱
✓ find_function_callers - 查找调用者
✓ query_call_chain      - 查询调用链
✓ visualize_code_graph  - 可视化代码

================================================================================
故障排查
================================================================================

问题: MCP 服务器无法启动
解决: uv pip install -e ".[mcp]"

问题: 找不到 uv 命令
解决: 确保 uv 在 PATH 中

问题: 路径错误
解决: 使用正斜杠 / 而不是反斜杠 \

================================================================================
文档
================================================================================

详细文档: CURSOR配置说明.md
完整指南: ../README_CURSOR_INTEGRATION.md
快速开始: ../如何在Cursor中使用.md

================================================================================
状态: ✅ 可用 | 版本: v2.0.0-alpha.1 | 测试: 通过
================================================================================
