{
  "$defs": {
    "auditorRuntime": {
      "additionalProperties": false,
      "properties": {
        "image_digest": {
          "$ref": "#/$defs/image"
        },
        "interpreter_sha256": {
          "$ref": "#/$defs/hash"
        },
        "module_sha256": {
          "$ref": "#/$defs/hash"
        }
      },
      "required": [
        "image_digest",
        "interpreter_sha256",
        "module_sha256"
      ],
      "type": "object"
    },
    "authority": {
      "additionalProperties": false,
      "properties": {
        "key_id": {
          "maxLength": 128,
          "minLength": 1,
          "type": "string"
        },
        "peer_uid": {
          "minimum": 0,
          "type": "integer"
        },
        "protocol": {
          "const": "no1-008a-audit-v1"
        },
        "public_key_hex": {
          "$ref": "#/$defs/hash"
        },
        "service_measurement": {
          "$ref": "#/$defs/hash"
        }
      },
      "required": [
        "key_id",
        "public_key_hex",
        "protocol",
        "peer_uid",
        "service_measurement"
      ],
      "type": "object"
    },
    "hash": {
      "pattern": "^[0-9a-f]{64}$",
      "type": "string"
    },
    "image": {
      "pattern": "^sha256:[0-9a-f]{64}$",
      "type": "string"
    },
    "role": {
      "additionalProperties": false,
      "properties": {
        "key_id": {
          "maxLength": 128,
          "minLength": 1,
          "type": "string"
        },
        "public_key_hex": {
          "$ref": "#/$defs/hash"
        }
      },
      "required": [
        "key_id",
        "public_key_hex"
      ],
      "type": "object"
    },
    "trusted": {
      "additionalProperties": false,
      "properties": {
        "auditor_runtime": {
          "$ref": "#/$defs/auditorRuntime"
        },
        "config_sha256": {
          "$ref": "#/$defs/hash"
        },
        "image_ids": {
          "additionalProperties": false,
          "properties": {
            "approver": {
              "$ref": "#/$defs/image"
            },
            "auditor": {
              "$ref": "#/$defs/image"
            },
            "executor": {
              "$ref": "#/$defs/image"
            },
            "producer": {
              "$ref": "#/$defs/image"
            },
            "verifier": {
              "$ref": "#/$defs/image"
            }
          },
          "required": [
            "producer",
            "executor",
            "approver",
            "auditor",
            "verifier"
          ],
          "type": "object"
        },
        "images": {
          "additionalProperties": false,
          "properties": {
            "approver": {
              "$ref": "#/$defs/image"
            },
            "auditor": {
              "$ref": "#/$defs/image"
            },
            "executor": {
              "$ref": "#/$defs/image"
            },
            "producer": {
              "$ref": "#/$defs/image"
            },
            "verifier": {
              "$ref": "#/$defs/image"
            }
          },
          "required": [
            "producer",
            "executor",
            "approver",
            "auditor",
            "verifier"
          ],
          "type": "object"
        },
        "inventory_sha256": {
          "additionalProperties": {
            "$ref": "#/$defs/hash"
          },
          "type": "object"
        },
        "plan_hashes": {
          "additionalProperties": {
            "$ref": "#/$defs/hash"
          },
          "type": "object"
        },
        "plan_set_hash": {
          "$ref": "#/$defs/hash"
        },
        "seccomp_sha256": {
          "$ref": "#/$defs/hash"
        },
        "source_snapshot_sha256": {
          "additionalProperties": {
            "$ref": "#/$defs/hash"
          },
          "type": "object"
        },
        "tool_sha256": {
          "$ref": "#/$defs/hash"
        }
      },
      "required": [
        "plan_set_hash",
        "plan_hashes",
        "inventory_sha256",
        "source_snapshot_sha256",
        "tool_sha256",
        "config_sha256",
        "seccomp_sha256",
        "images",
        "image_ids",
        "auditor_runtime"
      ],
      "type": "object"
    }
  },
  "$id": "https://tree-sitter-analyzer.invalid/schemas/no1-008a-public-config-v3.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "approver": {
      "$ref": "#/$defs/role"
    },
    "auditor": {
      "$ref": "#/$defs/authority"
    },
    "executor": {
      "$ref": "#/$defs/role"
    },
    "root_signature": {
      "pattern": "^[0-9a-f]{128}$",
      "type": "string"
    },
    "schema_version": {
      "const": 3
    },
    "trusted": {
      "$ref": "#/$defs/trusted"
    }
  },
  "required": [
    "schema_version",
    "executor",
    "approver",
    "auditor",
    "trusted",
    "root_signature"
  ],
  "type": "object"
}
